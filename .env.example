# Copy to .env and adjust as needed

APP_ENV=development
APP_BASE_URL=http://localhost:8000

# SQLite DB path (absolute or relative to project root)
DB_PATH=backend/database/app.sqlite

# Admin credentials
ADMIN_USER=admin
# For production, set ADMIN_PASSWORD_HASH (generated via password_hash)
ADMIN_PASSWORD=admin123
ADMIN_PASSWORD_HASH=
